## 2025-03-21 - [Keyboard Accessibility for Clickable Divs]
**Learning:** Found an accessibility anti-pattern where an interactive `div` (the campsite card content) only had an `onClick` handler. This prevented keyboard users from accessing the `CampsiteModal`. Adding `tabIndex={0}`, `role="button"`, an `aria-label`, and an `onKeyDown` listener that triggers on "Enter" or "Space" (with `e.preventDefault()` to stop page scrolling) successfully makes the element keyboard accessible and screen reader friendly.
**Action:** When inspecting components with click events, specifically review elements lacking semantic interactive roles (e.g., `div` or `span`) and ensure they include appropriate `tabIndex`, `role`, `aria-label`, and `onKeyDown` attributes to fully support keyboard navigation.
